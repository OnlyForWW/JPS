# config.yaml

# 目标文件夹路径
target_directory: "../config/"

# 用于筛选 YAML 文件的多个字符串
search_strings:
  - "attack_adv_bench_subset"

# 是否为每个修改的文件创建备份
backup: false

# 需要修改的键值对，以键路径为键，新的值为值
modifications:
  data_loader.dataset_name: 'advbench_subset'
  data_loader.dataset_path: '/workshop/crm/data/advbench_subset.csv'