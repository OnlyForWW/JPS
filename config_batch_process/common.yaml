# config.yaml

# 目标文件夹路径
target_directory: "../config/"

# 用于筛选 YAML 文件的多个字符串
search_strings:
  - "yaml"

# 是否为每个修改的文件创建备份
backup: false

# 需要修改的键值对，以键路径为键，新的值为值
modifications:
  model.agent_model_path: "/workshop/crm/checkpoint/Qwen2.5-14B-Instruct"

  attack_config.adv_save_dir: "/workshop/crm/project/IJTS/results/images"
  attack_config.revision_iteration_num: 5

  inference_config.inference_save_dir: "/workshop/crm/project/IJTS/results/generate"

  judge_config.judge_save_dir: "/workshop/crm/project/IJTS/results/eval"