# config.yaml

# 目标文件夹路径
target_directory: "../config/"

# 用于筛选 YAML 文件的多个字符串
search_strings:
  - "advbench"

# 是否为每个修改的文件创建备份
backup: false

# 需要修改的键值对，以键路径为键，新的值为值
modifications:
  data_loader.dataset_name: 'advbench'
  data_loader.dataset_path: '/workshop/crm/data/advbench/harmful_behaviors.csv'